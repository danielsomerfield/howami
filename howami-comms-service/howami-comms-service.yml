logging:
  level: INFO
  loggers:
    somerfield: DEBUG
  appenders:
    - type: console
      logFormat: "%-6level [%d{HH:mm:ss.SSS}] [%t] %logger{5} - %X{code} %msg %n"

testMode: ${TEST_MODE}
kafkaBootstrapServers: ${KAFKA_SERVICE_SERVICE_HOST:KAFKA_SERVICE_SERVICE_PORT}
