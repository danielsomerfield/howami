logging:
  level: INFO
  loggers:
    somerfield: DEBUG
  appenders:
    - type: console
      logFormat: "%-6level [%d{HH:mm:ss.SSS}] [%t] %logger{5} - %X{code} %msg %n"

mongoHost: ${MONGO_HOST}
kafkaBootstrapServers: ${KAFKA_SERVICE_SERVICE_HOST:-localhost}:${KAFKA_SERVICE_SERVICE_PORT:-9092}